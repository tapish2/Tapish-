def reverseList(A, start, end):
    while start < end :
        A[start], A[end] = A[end], A[start]
        start += 1
        end -= 1

A = [10, 20, 30, 40, 50, 60]
print(A)
reverseList(A, 0, 5)
print("Reverse list is : ")
print(A)
